// components/AIAssistants.js
export default function AIAssistants() {
  const assistantTypes = [
    "Coding Assistants", "Shell Assistants", "UI Generators", 
    "Documentation Generators", "Web & App Development"
  ]
  
  return (
    <section className="py-8 bg-white">
      <div className="container">
        <h2 className="text-2xl font-bold text-center mb-6">
          AI Development Tools & Assistants
        </h2>
        <div className="flex flex-wrap gap-4 justify-center">
          {assistantTypes.map((type, index) => (
            <div key={index} className="bg-gray-100 rounded-full px-4 py-2 text-sm flex items-center gap-2">
              <span>{type}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
